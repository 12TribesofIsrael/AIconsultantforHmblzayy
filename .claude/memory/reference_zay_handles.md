---
name: Zay handle map across platforms
description: Minister Zay's social handles differ across platforms — @hmblzayy on TT/Twitch vs @ministerzay on IG. Don't assume one handle works everywhere.
type: reference
originSessionId: 658350e4-0c5c-414d-bf11-3cdbafb30c35
modified: 2026-07-28T13:26:00.819Z
---
Minister Zay (Isaiah M. Thomas) does NOT use the same handle everywhere:

| Platform | Handle | URL |
|---|---|---|
| TikTok (main) | `@hmblzayy` | https://www.tiktok.com/@hmblzayy — 440K followers as of Jul 28, 2026 |
| TikTok (backup) | `@ministerzay` | https://www.tiktok.com/@ministerzay — 100.8K, linked from his main bio as "backup" |
| Twitch | `hmblzayy` | https://www.twitch.tv/hmblzayy |
| Instagram | **UNRESOLVED — see below** | |
| Twitter/X | `@minister_zay` | brand-only, low activity |

**⚠ Instagram broke (verified Jul 28, 2026).** Both `instagram.com/ministerzay` and
`instagram.com/hmblzayy` return "Sorry, this page isn't available" from a **logged-in**
persistent-profile session — so it is not a login wall. Renamed, removed, or blocking the
`@bmbaiautomations` account. IG keyword search for "hmbl zay" returns only third-party
repost accounts, no owned profile. **Don't burn passes retrying those two URLs** — ask
ShuggC for the current handle, or work from TikTok/Twitch, which are both healthy.
Note `@ministerzay` is now confirmed to be a **TikTok** account; the old "IG = @ministerzay"
mapping below is what led here.

**Why this matters:** I burned a scrape pass on `instagram.com/hmblzayy` (Apr 26 session) and hit a "Profile isn't available" page before checking `research/instagram/profile.md`. The IG handle is in the research repo — read it first. Don't assume the TikTok handle works for IG.

When asked for "his IG" or to scout/repost on IG, use `@ministerzay`. When asked for TT/Twitch, use `@hmblzayy`. The Faith Walk content lives on both, but the handles are not interchangeable.

His IG also links `@stayhumblestayhungryclothing` and `@hmblkicks` (brand accounts — different audiences than his personal `@ministerzay`).
